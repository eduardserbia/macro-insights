# === Airflow settings ===
# Set your local UID for Airflow container permissions
# On macOS/Linux you can get it with: id -u
AIRFLOW_UID=YOUR_LOCAL_UID

# === AWS S3 settings ===
# IAM credentials for accessing S3 (do NOT commit real values!)
AWS_ACCESS_KEY_ID=YOUR_AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY=YOUR_AWS_SECRET_ACCESS_KEY
AWS_DEFAULT_REGION=eu-central-1
S3_BUCKET=macro-insights-raw-data

# === ClickHouse settings ===
# Host URL of your ClickHouse Cloud cluster (from the "Connect" tab)
CH_HOST=YOUR_CLICKHOUSE_HOST
# Default secure port for ClickHouse Cloud is 8443
CH_PORT=8443
CH_USER=macro_user
CH_PASSWORD=YOUR_CLICKHOUSE_PASSWORD
# Default database name for dbt models
CH_DATABASE=macro_insights

# === dbt settings ===
# Path to dbt profiles directory inside the Airflow container
DBT_PROFILES_DIR=/opt/airflow/dbt/.ci

# === Optional Airflow configs ===
AIRFLOW__CORE__LOAD_EXAMPLES=false
AIRFLOW__CORE__EXECUTOR=LocalExecutor
